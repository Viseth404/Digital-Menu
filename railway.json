{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm ci && npm exec prisma generate -- --schema apps/web/prisma/schema.prisma && npm run build"
  },
  "deploy": {
    "preDeployCommand": [
      "npm exec prisma db push -- --schema apps/web/prisma/schema.prisma"
    ],
    "startCommand": "npm run start --workspace=apps/web",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
